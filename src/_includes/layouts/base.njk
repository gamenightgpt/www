<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "GameNightGPT - Board Game Rules Assistant" }}</title>
  <meta name="description" content="{{ description or 'Get instant, accurate answers to your rules questions. Your AI-powered board game assistant keeps the fun rolling.' }}">
  
  <!-- Favicon -->
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" type="image/x-icon" sizes="16x16" href="/favicon-16x16.ico">
  <link rel="icon" type="image/x-icon" sizes="48x48" href="/favicon-48x48.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="64x64" href="/favicon-64x64.png">
  <link rel="icon" href="/favicon.svg" type="image/svg+xml">
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400..1000;1,9..40,400..1000&family=Fraunces:ital,opsz,wght@0,9..144,100..900;1,9..144,100..900&family=Outfit:wght@100..900&display=swap" rel="stylesheet">
  
  <!-- CSS -->
  <link rel="stylesheet" href="/css/style.css">
  
  <!-- RSS Feed -->
  <link rel="alternate" type="application/rss+xml" title="{{ site.title }} RSS Feed" href="/rss.xml">
  
  <!-- Canonical URL -->
  <link rel="canonical" href="{{ site.url }}{{ page.url }}">

  <!-- Open Graph -->
  <meta property="og:title" content="{{ title or 'GameNightGPT - Board Game Rules Assistant' }}">
  <meta property="og:description" content="{{ description or 'Get instant, accurate answers to your rules questions. Your AI-powered board game assistant keeps the fun rolling.' }}">
  <meta property="og:type" content="{{ ogType or 'website' }}">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:image" content="{{ ogImage or site.url + site.ogImage }}">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:site_name" content="GameNightGPT">
  <meta property="og:locale" content="en_US">
  {%- if ogType == 'article' %}
  <meta property="article:published_time" content="{{ date | dateToRfc3339 }}">
  <meta property="article:author" content="{{ site.author.name }}">
  {%- endif %}

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{ title or 'GameNightGPT - Board Game Rules Assistant' }}">
  <meta name="twitter:description" content="{{ description or 'Get instant, accurate answers to your rules questions. Your AI-powered board game assistant keeps the fun rolling.' }}">
  <meta name="twitter:image" content="{{ ogImage or site.url + site.ogImage }}">

  <!-- Structured Data -->
  {% include "partials/json-ld.njk" %}

  <!-- Plausible analytics (with tagged-events + outbound-links) -->
  <script defer data-domain="gamenightgpt.com" src="https://plausible.io/js/script.tagged-events.outbound-links.js"></script>  
</head>
<body>
  <a href="#main" class="skip-to-content">Skip to main content</a>
  
  {{ content | safe }}
  
  <!-- Newsletter JavaScript -->
  <script src="/js/newsletter.js" defer></script>
</body>
</html>