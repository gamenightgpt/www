<section class="pt-24 pb-12 md:pt-32 md:pb-16 overflow-hidden">
  <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
    <div class="grid md:grid-cols-2 gap-8 md:gap-12 items-center">
      <!-- Text content -->
      <div class="text-center md:text-left">
        <h1 class="text-[var(--neutral-900)] mb-6">
          Get Answers Fast &mdash; Keep Gaming
        </h1>
        <p class="text-lg md:text-xl text-[var(--neutral-700)] mb-8 leading-relaxed">
          Get instant, accurate answers to your rules questions. <br/>
          Your AI-powered board game assistant keeps the fun rolling.
        </p>
        <button
          onclick="document.getElementById('signup').scrollIntoView({ behavior: 'smooth' })"
          class="btn-primary text-lg px-8 py-4"
          aria-label="Join the waitlist for early access"
        >
          Join the Waitlist
        </button>
      </div>
      
      <!-- Illustration -->
      <div class="relative mt-8 md:mt-0">
        <div class="relative max-w-lg mx-auto">
          <!-- Floating chat bubble -->
          <div class="absolute -top-20 right-4 md:right-12 animate-float z-10">
            <div class="relative bg-white rounded-2xl shadow-lg px-3 py-2 md:px-4 md:py-3 border-2 border-[var(--primary-500)] max-w-[200px] md:max-w-[240px]">
              <div class="flex items-center gap-2">
                <span class="text-xl md:text-2xl">✨</span>
                <p class="text-xs md:text-sm font-medium text-[var(--neutral-900)]">
                  "Can I build a settlement here?"
                </p>
              </div>
              <!-- Chat bubble tail -->
              <div class="absolute -bottom-2 left-6 w-4 h-4 bg-white border-b-2 border-r-2 border-[var(--primary-500)] transform rotate-45"></div>
            </div>
          </div>
          
          <!-- Hero image -->
          <div class="mt-16">
            <img 
              src="/images/hero-small.webp" 
              alt="GameNightGPT" 
              class="w-[85%] md:w-[60%] h-auto mx-auto"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>