<section id="signup" class="py-8 lg:py-12 bg-noise" style="background-color: var(--bg-dark-coffee); color: var(--text-on-dark);">
  <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
    <div class="max-w-2xl mx-auto text-center">
      <h2 class="text-[26px] font-normal mb-2" style="font-size: 26px !important;">
        Get updates when we add new games
      </h2>
      <p class="text-lg opacity-80 mb-6">
        Occasional updates. Unsubscribe anytime.
      </p>
      
      <div id="signup-success" class="hidden bg-white/10 backdrop-blur-sm rounded-lg p-8 mb-8 text-white">
        <span class="text-4xl mb-4 block">🎉</span>
        <h3 class="font-bold text-xl mb-2">You're on the list!</h3>
        <p class="opacity-80">
          We'll keep you posted on new game additions.
        </p>
      </div>
      
      <form id="newsletter-form" class="flex flex-col sm:flex-row gap-4 max-w-lg mx-auto">
        <input
          type="email"
          name="email"
          id="email-input"
          placeholder="Email address"
          required
          aria-label="Email address"
          class="flex-1 px-6 py-3 rounded-lg text-white border border-[#5D4037] placeholder-gray-400 focus:border-[var(--accent-orange)] focus:ring-1 focus:ring-[var(--accent-orange)] focus:outline-none transition-all shadow-inner"
          style="background-color: rgba(0,0,0,0.3);"
        />
        <button
          type="submit"
          id="submit-button"
          class="px-8 py-3 rounded-lg font-bold text-white text-lg btn-3d-orange"
        >
          Subscribe
        </button>
      </form>
      
      <p id="error-message" class="hidden mt-4 text-red-300">
        Something went wrong. Please try again.
      </p>
    </div>
  </div>
  
  <!-- Decorative star/sparkle in bottom right of footer as seen in image -->

</section>
