<section id="signup" class="py-12 md:py-16 lg:py-20 bg-[var(--primary-500)]">
  <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
    <div class="max-w-2xl mx-auto text-center">
      <h2 class="text-white mb-4">
        Join the Waitlist
      </h2>
      <p class="text-xl text-[var(--primary-100)] mb-8">
	<strong>get free access through November 1</strong>
      </p>
      <p class="text-xl text-[var(--primary-100)] mb-8">
        and subscribe to our newsletter so you get updates as we level up.
      </p>
      
      <div id="signup-success" class="hidden bg-white/20 backdrop-blur-sm rounded-lg p-8 text-white">
        <span class="text-4xl mb-4 block">🎉</span>
        <h3 class="text-white mb-2">You're on the list!</h3>
        <p class="text-[var(--primary-100)]">
          Check your email to confirm. We'll notify you when it's your turn to join.
        </p>
      </div>
      
      <form id="newsletter-form" class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
        <input
          type="email"
          name="email"
          id="email-input"
          placeholder="your@email.com"
          required
          aria-label="Email address"
          class="flex-1 px-6 py-4 rounded-lg text-[var(--neutral-900)] placeholder-[var(--neutral-500)] focus:outline-none focus:ring-4 focus:ring-white/30"
        />
        <button
          type="submit"
          id="submit-button"
          class="bg-white text-[var(--primary-500)] px-6 sm:px-8 py-4 rounded-lg font-semibold hover:bg-[var(--primary-50)] hover:text-[var(--primary-700)] hover:transform hover:-translate-y-0.5 hover:shadow-lg focus:outline-none focus:ring-4 focus:ring-white/30 transition-all duration-200 min-h-[56px] flex items-center justify-center"
        >
          Get Early Access
        </button>
      </form>
      
      <p id="error-message" class="hidden mt-4 text-white/90">
        Something went wrong. Please try again or email us at dallan@gamenightgpt.com
      </p>
      
      <p class="mt-6 text-sm text-[var(--primary-100)]">
        Unsubscribe anytime.
      </p>
    </div>
  </div>
</section>
