---
layout: layouts/base.njk
title: GameNightGPT for Publishers - White-Label Rules Assistant
description: Expand your board game's reach with AI-powered rules support. Make complex games accessible, create a marketing channel, zero setup required.
permalink: /backstage/
---

{% include "partials/navbar.njk" %}

<main id="main">
  <!-- Hero Section -->
  <section class="pt-24 pb-12 md:pt-32 md:pb-16">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <div class="text-center max-w-4xl mx-auto">
        <h1 class="text-[var(--neutral-900)] mb-6">
          Expand Your Game's Reach with<br/>AI-Powered Rules Support
        </h1>
        <p class="text-xl md:text-2xl text-[var(--neutral-700)] mb-8 leading-relaxed text-center mx-auto max-w-2xl">
          Make complex games accessible to new players. Zero setup required.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a href="#publisher-waitlist" class="btn-primary text-lg px-8 py-4">
            Join Publisher Waitlist
          </a>
          <a href="#schedule-demo" class="text-[var(--primary-700)] hover:text-[var(--accent-800)] font-medium">
            or schedule a demo →
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Problem/Solution Section -->
  <section class="py-12 md:py-16 lg:py-20 bg-[var(--neutral-100)]">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <div class="grid md:grid-cols-2 gap-12 items-start">
        <!-- The Challenge -->
        <div>
          <h2 class="text-[var(--neutral-900)] mb-6">The Challenge</h2>
          <div class="md:pt-8">
            <p class="text-[var(--neutral-700)] mb-6 italic">Every publisher faces these...</p>
            <div class="space-y-4">
            <div class="flex items-start gap-3">
              <span class="text-2xl mt-1">🧊</span>
              <p class="text-[var(--neutral-700)]">
                <strong>Games sit on the shelf of shame.</strong> Your amazing game sits on the shelf because the rulebook looks daunting.
              </p>
            </div>
            <div class="flex items-start gap-3">
              <span class="text-2xl mt-1">⏱</span>
              <p class="text-[var(--neutral-700)]">
                <strong>Lack of table-readiness limits discovery.</strong> Games that take too long to learn / teach don’t get recommended or shared.
              </p>
            </div>
            <div class="flex items-start gap-3">
              <span class="text-2xl mt-1">🌐</span>
              <p class="text-[var(--neutral-700)]">
                <strong>Missed marketing opportunities.</strong> Players never return to your website to see your new games.
              </p>
            </div>
            </div>
          </div>
        </div>
        
        <!-- Your Solution -->
        <div>
          <h2 class="text-[var(--primary-500)] mb-6">Your White-Label Solution</h2>
          <div class="bg-white rounded-xl p-8 shadow-sm border border-[var(--neutral-300)]">
            <div class="flex items-center gap-3 mb-4">
              <span class="text-3xl">🎯</span>
              <h3 class="text-[var(--neutral-900)]">rules.yourgame.com</h3>
            </div>
            <p class="text-[var(--neutral-700)] mb-6">
              A custom AI assistant that lives on your domain, answers your players' questions, and brings them back to your website.
            </p>
            <ul class="space-y-3">
              <li class="flex items-start gap-2">
                <span class="text-[var(--primary-500)] mt-1">✓</span>
                <span class="text-[var(--neutral-700)]">Instant answers keep players engaged</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-[var(--primary-500)] mt-1">✓</span>
                <span class="text-[var(--neutral-700)]">Players return to YOUR site for help</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-[var(--primary-500)] mt-1">✓</span>
                <span class="text-[var(--neutral-700)]">Showcase new games to existing customers</span>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="py-12 md:py-16 lg:py-20">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <h2 class="text-center text-[var(--neutral-900)] mb-12">
        How It Works
      </h2>
      <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
        <div class="text-center">
          <div class="text-5xl mb-4">✨</div>
          <h3 class="text-[var(--neutral-900)] mb-3">We Build Your Assistant</h3>
          <p class="text-[var(--neutral-700)]">
            Our AI learns all your games' rules, FAQs, and official clarifications.
          </p>
        </div>
        <div class="text-center">
          <div class="text-5xl mb-4">🔗</div>
          <h3 class="text-[var(--neutral-900)] mb-3">You Add One Link</h3>
          <p class="text-[var(--neutral-700)]">
            Simply link to your white-label rules assistant from your website.
          </p>
        </div>
        <div class="text-center">
          <div class="text-5xl mb-4">📈</div>
          <h3 class="text-[var(--neutral-900)] mb-3">Players Get Help, You Get Data</h3>
          <p class="text-[var(--neutral-700)]">
            Track what players ask about and showcase new products.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Value Props -->
  <section class="py-12 md:py-16 lg:py-20 bg-[var(--primary-50)]">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <h2 class="text-center text-[var(--neutral-900)] mb-4">
        Why Choose GameNightGPT
      </h2>
      <p class="text-center text-[var(--neutral-700)] mb-12 max-w-3xl mx-auto">
        Make your games more accessible, delight your players, and gain insights that deepen engagement.
      </p>
      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-white rounded-xl p-8 text-center shadow-sm">
          <div class="text-5xl mb-4">📈</div>
          <h3 class="text-[var(--neutral-900)] mb-3">Grow Your Audience</h3>
          <p class="text-[var(--neutral-700)]">
            Make complex games more accessible. Remove the intimidation factor that limits your market.
          </p>
        </div>
        <div class="bg-white rounded-xl p-8 text-center shadow-sm">
          <div class="text-5xl mb-4">🎯</div>
          <h3 class="text-[var(--neutral-900)] mb-3">Marketing Channel</h3>
          <p class="text-[var(--neutral-700)]">
            Turn rules questions into sales opportunities. Showcase expansions and new games to engaged players.
          </p>
        </div>
        <div class="bg-white rounded-xl p-8 text-center shadow-sm">
          <div class="text-5xl mb-4">⭐</div>
          <h3 class="text-[var(--neutral-900)] mb-3">Happy Players</h3>
          <p class="text-[var(--neutral-700)]">
            Better player experience leads to better reviews, more recommendations, and stronger brand loyalty.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Early Access CTA -->
  <section class="py-12 md:py-16 lg:py-20">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <div class="bg-[var(--accent-50)] rounded-2xl p-8 md:p-12 text-center">
        <h2 class="text-[var(--neutral-900)] mb-4">
          Shape the Future of Board Game Publishing
        </h2>
        <p class="text-lg text-[var(--neutral-700)] mb-8 max-w-3xl mx-auto">
          We're inviting a select group of innovative publishers to help shape our platform.
          Get early access &mdash; and help define features that matter most to you.
        </p>
        <div class="inline-flex items-center gap-2 text-[var(--accent-800)] font-medium">
          <span class="text-2xl">🚀</span>
          <span>Limited spots available before our official launch</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Dual CTA Section -->
  <section id="publisher-waitlist" class="py-12 md:py-16 lg:py-20 bg-[var(--neutral-900)]">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <div class="grid md:grid-cols-2 gap-12 max-w-5xl mx-auto">
        <!-- Waitlist Form -->
        <div class="bg-white rounded-xl p-8">
          <h3 class="text-[var(--neutral-900)] mb-4">Join the Publisher Waitlist</h3>
          <p class="text-[var(--neutral-700)] mb-6">
            and subscribe to our newsletter so you get updates as we level up.
          </p>
          
          <form id="publisher-form" class="space-y-4">
            <input
              type="text"
              name="name"
              placeholder="Your name"
              required
              class="w-full px-4 py-3 rounded-lg border-2 border-[var(--neutral-300)] focus:border-[var(--primary-500)] focus:outline-none"
            />
            <input
              type="text"
              name="company"
              placeholder="Company name"
              required
              class="w-full px-4 py-3 rounded-lg border-2 border-[var(--neutral-300)] focus:border-[var(--primary-500)] focus:outline-none"
            />
            <input
              type="email"
              name="email"
              placeholder="Work email"
              required
              class="w-full px-4 py-3 rounded-lg border-2 border-[var(--neutral-300)] focus:border-[var(--primary-500)] focus:outline-none"
            />
            <button
              type="submit"
              class="w-full btn-primary"
            >
              Request Early Access
            </button>
          </form>
          
          <div id="publisher-success" class="hidden">
            <div class="bg-[var(--primary-50)] rounded-lg p-6 text-center">
              <span class="text-4xl mb-4 block">🎉</span>
              <h4 class="text-[var(--neutral-900)] mb-2">You're on the list!</h4>
              <p class="text-[var(--neutral-700)]">
                Check your email to confirm. We'll reach out soon with more information.
              </p>
            </div>
          </div>
          
          <p id="publisher-error" class="hidden mt-4 text-red-600">
            Something went wrong. Please email us at dallan@gamenightgpt.com
          </p>
        </div>
        
        <!-- Schedule Call -->
        <div id="schedule-demo" class="bg-[var(--neutral-800)] rounded-xl p-8 text-white">
          <h3 class="text-white mb-4">Prefer to Talk?</h3>
          <p class="text-[var(--neutral-300)] mb-6">
            Schedule a zoom call to see how GameNightGPT can work for your games.
          </p>
          <a 
            href="https://calendly.com/dallan-gamenightgpt/gamenightgpt-white-label" 
            target="_blank"
            class="inline-flex items-center gap-2 px-6 py-3 bg-white text-[var(--neutral-900)] rounded-lg font-semibold hover:bg-[var(--neutral-100)] transition-all duration-200"
          >
            Schedule Call
            <span>📅</span>
          </a>
          <p class="text-sm text-[var(--neutral-400)] mt-6">
            We're early stage and would love your input on how to best serve publishers like you.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="py-12 md:py-16 lg:py-20">
    <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
      <h2 class="text-center text-[var(--neutral-900)] mb-12">
        Publisher FAQ
      </h2>
      <div class="max-w-3xl mx-auto">
        <div class="faq-item border-b border-[var(--neutral-300)]">
          <button
            onclick="toggleFAQ(0)"
            class="w-full py-6 flex items-center justify-between text-left hover:bg-[var(--neutral-100)] rounded-lg px-4 -mx-4 transition-all duration-200"
            aria-expanded="false"
            aria-controls="faq-answer-0"
          >
            <h3 class="text-[var(--neutral-900)] pr-4 text-left">What games do you support?</h3>
            <span class="faq-icon text-3xl text-[var(--primary-500)] transition-transform flex-shrink-0 w-12 h-12 flex items-center justify-center">+</span>
          </button>
          <div id="faq-answer-0" class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
            <p class="text-[var(--neutral-700)] px-4 pb-6">
              We can support your entire catalog. Our AI learns from your rulebooks, FAQs, and official clarifications
              to provide accurate answers for all your games, from simple to complex.
            </p>
          </div>
        </div>
        
        <div class="faq-item border-b border-[var(--neutral-300)]">
          <button
            onclick="toggleFAQ(1)"
            class="w-full py-6 flex items-center justify-between text-left hover:bg-[var(--neutral-100)] rounded-lg px-4 -mx-4 transition-all duration-200"
            aria-expanded="false"
            aria-controls="faq-answer-1"
          >
            <h3 class="text-[var(--neutral-900)] pr-4 text-left">How does white-labeling work?</h3>
            <span class="faq-icon text-3xl text-[var(--primary-500)] transition-transform flex-shrink-0 w-12 h-12 flex items-center justify-center">+</span>
          </button>
          <div id="faq-answer-1" class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
            <p class="text-[var(--neutral-700)] px-4 pb-6">
              Your rules assistant lives at a subdomain like rules.yourgame.com or qa.yourstudio.com. It features your branding and seamlessly integrates with your website. Players never leave your digital ecosystem.
            </p>
          </div>
        </div>
        
        <div class="faq-item border-b border-[var(--neutral-300)]">
          <button
            onclick="toggleFAQ(2)"
            class="w-full py-6 flex items-center justify-between text-left hover:bg-[var(--neutral-100)] rounded-lg px-4 -mx-4 transition-all duration-200"
            aria-expanded="false"
            aria-controls="faq-answer-2"
          >
            <h3 class="text-[var(--neutral-900)] pr-4 text-left">What about pricing?</h3>
            <span class="faq-icon text-3xl text-[var(--primary-500)] transition-transform flex-shrink-0 w-12 h-12 flex items-center justify-center">+</span>
          </button>
          <div id="faq-answer-2" class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
            <p class="text-[var(--neutral-700)] px-4 pb-6">
              We're finalizing our pricing model with early partners. Our goal is to make this accessible for publishers of all sizes, with plans based on your catalog size and player engagement levels.
            </p>
          </div>
        </div>
        
        <div class="faq-item">
          <button
            onclick="toggleFAQ(3)"
            class="w-full py-6 flex items-center justify-between text-left hover:bg-[var(--neutral-100)] rounded-lg px-4 -mx-4 transition-all duration-200"
            aria-expanded="false"
            aria-controls="faq-answer-3"
          >
            <h3 class="text-[var(--neutral-900)] pr-4 text-left">When will this be available?</h3>
            <span class="faq-icon text-3xl text-[var(--primary-500)] transition-transform flex-shrink-0 w-12 h-12 flex items-center justify-center">+</span>
          </button>
          <div id="faq-answer-3" class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
            <p class="text-[var(--neutral-700)] px-4 pb-6">
              We're onboarding select early access partners now, with broader availability planned for later this year. Join the waitlist to secure your spot and help shape the platform.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</main>

{% include "partials/footer.njk" %}

<script>
// Publisher form handling
document.addEventListener('DOMContentLoaded', function() {
  const form = document.getElementById('publisher-form');
  const successDiv = document.getElementById('publisher-success');
  const errorMessage = document.getElementById('publisher-error');
  
  if (form) {
    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const formData = new FormData(form);
      const email = formData.get('email');
      const name = formData.get('name');
      const company = formData.get('company');
      
      // Submit to Buttondown with proper metadata format
      const buttondownData = new FormData();
      buttondownData.append('email', email);
      buttondownData.append('metadata__name', name);
      buttondownData.append('metadata__company', company);
      buttondownData.append('metadata__type', 'publisher');
      buttondownData.append('tag', 'publisher');
      buttondownData.append('embed', '1');
      
      try {
        const response = await fetch('https://buttondown.com/api/emails/embed-subscribe/gamenightgpt', {
          method: 'POST',
          body: buttondownData,
        });
        
        if (response.ok) {
          form.style.display = 'none';
          successDiv.classList.remove('hidden');
        } else {
          errorMessage.classList.remove('hidden');
        }
      } catch (error) {
        errorMessage.classList.remove('hidden');
      }
    });
  }
});

// Reuse FAQ toggle function
function toggleFAQ(index) {
  const button = event.currentTarget;
  const answer = document.getElementById(`faq-answer-${index}`);
  const icon = button.querySelector('.faq-icon');
  const isOpen = button.getAttribute('aria-expanded') === 'true';
  
  // Close all other FAQs
  document.querySelectorAll('.faq-item').forEach((item, i) => {
    if (i !== index) {
      const otherButton = item.querySelector('button');
      const otherAnswer = item.querySelector('.faq-answer');
      const otherIcon = item.querySelector('.faq-icon');
      otherButton.setAttribute('aria-expanded', 'false');
      otherAnswer.style.maxHeight = '0';
      otherIcon.style.transform = 'rotate(0deg)';
    }
  });
  
  // Toggle current FAQ
  if (isOpen) {
    button.setAttribute('aria-expanded', 'false');
    answer.style.maxHeight = '0';
    icon.style.transform = 'rotate(0deg)';
  } else {
    button.setAttribute('aria-expanded', 'true');
    answer.style.maxHeight = answer.scrollHeight + 'px';
    icon.style.transform = 'rotate(45deg)';
  }
}
</script>
